# ig-autobot Full Code Audit
Date: 2026-08-18
Scope: every source/config/workflow file under `C:\Users\Huawei\Downloads\ig-autobot`
Context: Telegram session history + loaded `ig-autobot` skill/memory

---

## Executive Summary
- Main workflow: `master_content_gen.yml` Thu/Sun 02:00 UTC → `prepare_assets.py` per platform → `publish_*.py`
- Wilma workflow: `master_wilma_gen.yml` Thu/Sun 03:00 UTC → `forwilma/wilma_bot.py --mode generate_all` → `forwilma/publish_wilma_*.py`
- Current state: main `state.json` queue=0, active=None; Wilma queue=3, active=dict, day_index=4
- Branch strategy: isolation via state-path scoping on `master`; separate long-lived branch not adopted.

---

## Confirmed Bugs / Deficiencies (Ranked)

### P0 — Fix First
1. **bot.py duplicate dead code in `_generate_text_ai_horde`**
   - File: `bot.py:1981-2100+`
   - Two identical AI-Horde text blocks; first can return, second is unreachable dead code. Maintenance hazard.
   - Status: FIXED — duplicate block removed.
2. **Wilma fallback import boundary incomplete**
   - File: `forwilma/wilma_bot.py:22-46`
   - If `bot.py` import fails, `clean_caption_formatting` is not guaranteed in fallback namespace, but later code calls it. Risk: `NameError` in CI.
   - Status: FIXED — fallback stubs now cover all later-used symbols.
3. **Workflow git hygiene violations**
   - Files:
     - `.github/workflows/auto_bluesky.yml:40,44`
     - `.github/workflows/auto_instagram.yml:109`
     - `.github/workflows/master_wilma_gen.yml:44`
     - `.github/workflows/hyperframes_render.yml:66-67`
     - `.github/workflows/master_content_gen.yml:45`
   - Issues: `git pull --rebase`, missing `git add -A`, inconsistent force-add of ignored assets, insufficient conflict resolution scope.
   - Status: PARTIALLY FIXED — main + Wilma poster workflows normalized; `hyperframes_render.yml` and `master_content_gen.yml` left for next pass to avoid overreach.
4. **`prepare_assets.py` queue mutation while iterating**
   - File: `scripts/prepare_assets.py:41-52`
   - Pops candidate by index during iteration; can skip next bundle after removal.
   - Status: FIXED — index chosen before pop.

### P1 — Fix Soon
5. **Wilma LinkedIn publish success/failure contract inconsistency**
   - File: `forwilma/publish_wilma_linkedin.py:170-204, 300-323`
   - Carousel path returns True/raises; single-image path returns True/False. On `False`, state/flag are not updated → stale active bundle + stale flag.
   - Status: FIXED — single-image helper now raises on failure, preserving existing state semantics.
6. **Bluesky/Wilma caption truncation mismatch**
   - Files: `scripts/publish_bluesky.py`, `forwilma/publish_wilma_bluesky.py`, `forwilma/wilma_bot.py`
   - Editor limit 250, hard total 300, publisher truncates at 300. Mid-sentence cuts still possible.
   - Status: NOT STARTED
7. **Wilma persona regex coverage gaps**
   - File: `forwilma/wilma_bot.py:337-375`
   - Missing: sentence-initial capitals, “our N-year-old”, plural invented ages, punctuation-boundary variants.
   - Status: FIXED — expanded patterns added.
8. **Local-media bypass missing for Threads/YouTube**
   - Files: `scripts/publish_threads.py`, `scripts/publish_youtube.py`
   - Still gate on Pages URL liveness; if local prepared file exists, should skip URL check.
   - Status: PARTIALLY FIXED — Threads now prefers local `reel.mp4`/`output.jpg`; YouTube already uses local `reel.mp4`, no change needed.

### P2 — Backlog
9. **Main Instagram `state.json` queue empty / active=None**
   - File: `state.json`
   - Symptom of generator/poster drift; needs queue refill from remote master gen.
   - Status: NOT STARTED
10. **Light/misaligned test coverage**
    - `tests/test_bundle_reservation.py`, `tests/test_platform_tailoring.py`, `tests/test_prepare_assets_queue.py`, `tests/test_verify_outputs.py`
    - Missing: queue mutation safety, Windows backslash normalization, stale-artifact cleanup, fallback import path.
    - Status: PARTIALLY FIXED — added queue mutation regression test; fixed misaligned Threads reservation test.
11. **Stale local artifacts risk**
    - Repo root: `output.jpg`, `story.jpg`, `post_reel.flag`, `post_story.flag`, `linkedin_ready.flag`
    - `prepare_assets.py` now clears stale files, but post-prep failures can leave flags behind.
    - Status: NOT STARTED
12. **HyperFrames render fragility on Windows**
    - Files: `hyperframes/compositions/*`, `scripts/render_hyperframes_reel.py`, `.github/workflows/hyperframes_render.yml`
    - Known: `timeline_id_mismatch`, headless Chrome/GPU hangs, rebase-based push.
    - Status: NOT STARTED
13. **Bare/silent except + missing timeouts**
    - 6 bare `except:`; 1 `except Exception: pass`; 34 request call sites without `timeout=`; 2 `open()` without `encoding=`.
    - Status: NOT STARTED
14. **`update_bundles.py` Threads rules mismatch**
    - File: `scripts/update_bundles.py:70-78`
    - Forces Bluesky-style CTA onto Threads; publisher does not validate/restore it.
    - Status: NOT STARTED
15. **LinkedIn carousel day guard duplicated**
    - Files: `scripts/prepare_assets.py:357-360`, `scripts/publish_linkedin.py:191-194`
    - Duplicate enforcement; drift risk.
    - Status: NOT STARTED

---

## Ranked Fix Patch List

### Patch 1 — Remove dead duplicate text-gen block in `bot.py`
- Target: `bot.py:1981-2100+`
- Change: delete the second identical AI-Horde text block; keep single canonical submit/poll/fallback path.
- Status: COMPLETED

### Patch 2 — Harden Wilma fallback imports
- Target: `forwilma/wilma_bot.py:22-46`
- Change: ensure fallback namespace defines `clean_caption_formatting`, `_ai_verify_caption`, `_editor_fallback`, `sanitize_image_prompt`, `extract_hook_text`, `_get_available_horde_text_models` if `bot.py` import fails.
- Status: COMPLETED

### Patch 3 — Fix queue mutation in `prepare_assets.py`
- Target: `scripts/prepare_assets.py:41-52`
- Change: build candidate list first; return from non-mutated index; do not `pop(index)` while iterating.
- Status: COMPLETED

### Patch 4 — Normalize workflow git hygiene
- Targets:
  - `.github/workflows/auto_bluesky.yml`
  - `.github/workflows/auto_instagram.yml`
  - `.github/workflows/auto_linkedin.yml`
  - `.github/workflows/auto_threads.yml`
  - `.github/workflows/auto_youtube.yml`
  - `.github/workflows/auto_pinterest.yml`
  - `.github/workflows/auto_wilma_bluesky.yml`
  - `.github/workflows/auto_wilma_linkedin.yml`
  - `.github/workflows/master_content_gen.yml`
  - `.github/workflows/master_wilma_gen.yml`
  - `.github/workflows/hyperframes_render.yml`
- Change pattern:
  - Replace `git pull --rebase` with `git pull --no-rebase`
  - Use `git add -A`
  - Force-add ignored runtime outputs when needed: `git add -f output.jpg caption.txt || true`
  - Commit tolerate-empty: `git commit -m "..." || true`
  - Push retry: `git push origin master || (sleep 5 && git pull --no-rebase origin master && git push)`
  - For master gen: broaden conflict resolution to images/reels/flags/carousel.json
- Status: PARTIALLY COMPLETED — main + Wilma poster workflows normalized; `hyperframes_render.yml` and `master_content_gen.yml` left for next pass.

### Patch 5 — Fix Wilma LinkedIn success/failure contract
- Target: `forwilma/publish_wilma_linkedin.py:170-323`
- Change: make single-image helper return bool consistently; on `False`, print error and `sys.exit(1)` rather than falling through without state update.
- Status: COMPLETED

### Patch 6 — Expand Wilma persona regex
- Target: `forwilma/wilma_bot.py:337-375`
- Change: add patterns for capitalized sentence-initial variants, “our N-year-old”, plural ages, and punctuation-adjacent forms.
- Status: COMPLETED

### Patch 7 — Local-media bypass for Threads/YouTube
- Target: `scripts/publish_threads.py`, `scripts/publish_youtube.py`
- Change: if local prepared media file exists, skip `check_url_live()` and publish directly.
- Status: PARTIALLY COMPLETED — Threads updated; YouTube already local-first.

### Patch 8 — Add regression tests
- Targets: `tests/`
- Add: queue mutation safety, Windows backslash normalization persistence, stale-artifact cleanup, Wilma fallback import path.
- Status: PARTIALLY COMPLETED — queue mutation test added; bundle reservation test corrected; remaining tests still TODO.

---

## Isolation Recommendation
- A separate branch is not required for isolation.
- Safer option: keep main and Wilma workflows on `master`, but split workflow ownership:
  - main posters use `state.json`
  - Wilma posters use `forwilma/state.json`
- This avoids merge conflicts entirely without maintaining a parallel branch.
